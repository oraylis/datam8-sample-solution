# Macro Import ----------
{% import '__collections/macro/column_declaration_stage.jinja2' as column_declaration %}
{% import "__collections/macro/common_python_snippets.py.jinja2" as py_snippets %}

{%- for entity in model.get_entity_list_by_layer('stage') %}
  {# V2 unified structure - entity is UnifiedEntityFactory #}
  {%- set table = entity.entity %}
  {%- set layer_name = "stage" %}
  {%- set data_product = entity.data_product %}
  {%- set data_module = entity.data_module %}
  {%- set entity_name = entity.entity_name %}
  {%- set full_table_name = data_product|string + "_" + data_module|string + "_" + entity_name|string %}
  {%- set layer_full_table_name = layer_name + "." + full_table_name %}
  {%- set file_path = helper.build_path("notebooks", SystemProperties().stage_folder, "ddl", data_product|string, data_module|string, entity_name|string) %}
>>>>>>>>>> {{ file_path }}.py | py
# Databricks notebook source
# MAGIC %md
# MAGIC # DDL for {{layer_name}}.{{full_table_name}}

# COMMAND ----------

{% include "__collections/include/ddl_type_mapping.py.jinja2" %}

# COMMAND ----------

# MAGIC %md
# MAGIC ## Get variable values

# COMMAND ----------

{{ py_snippets.common_variables(layer_name, entity, SystemProperties, model) }}

# COMMAND ----------

{{- column_declaration.ddl_struct_type(model, table.attribute) }}

# COMMAND ----------

# DBTITLE 1,Define new partitions
{%- set partitions = (
    "__Year",
    "__Month",
    "__Day",
    "__InsertTimestampUTC",
) %}
new_partitions = (
  {%- for p in partitions %}
  "{{p}}",
  {%- endfor %}
)

# COMMAND ----------

# DBTITLE 1,Define DDL statement
ddl = """
CREATE TABLE `%(catalog)s`.`%(schema)s`.`%(table)s`
(
    {{ column_declaration.ddl_column_declaration(model, table.attribute) }}
)
USING DELTA
PARTITIONED BY (
{%- for p in partitions %}
    `{{p}}`{{ "," if not loop.last }}
{%- endfor %}
)
""" % {
    "catalog": catalog.name,
    "schema": zone,  # Uses ZonesFactory target name via zone_mapping
    "table": full_table_name,
}

# COMMAND ----------

# DBTITLE 1,Define refactored columns
refactored_columns = []

# COMMAND ----------

{% include "__collections/include/ddl_schema_migration.py.jinja" %}

# COMMAND ----------

<<<<<<<<<< {{ file_path }}.py | py
{%- endfor -%}
