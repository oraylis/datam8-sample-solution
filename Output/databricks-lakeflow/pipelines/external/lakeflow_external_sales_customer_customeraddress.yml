resources:
  pipelines:
    lakeflow_external_sales_customer_customeraddress:
      name: ${var.job_name_prefix}Lakeflow External Sales_Customer_CustomerAddress
      catalog: ${var.catalog_name}
      schema: ${var.lakeflow_schema_prefix}raw
      serverless: true
      channel: PREVIEW
      configuration:
        catalog_name: ${var.catalog_name}
        schema_prefix: ${var.lakeflow_schema_prefix}
      libraries:
        - notebook:
            path: ${workspace.root_path}/${var.lakeflow_output_path}/pipelines/notebooks/external/Sales/Customer/CustomerAddress