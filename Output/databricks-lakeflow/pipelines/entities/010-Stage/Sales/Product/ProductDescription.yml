resources:
  pipelines:
    lakeflow_entity_stage_sales_product_productdescription:
      name: ${var.job_name_prefix}Lakeflow Entity stage Sales_Product_ProductDescription
      catalog: ${var.catalog_name}
      schema: ${var.lakeflow_schema_prefix}stage
      serverless: true
      channel: PREVIEW
      configuration:
        catalog_name: ${var.catalog_name}
        schema_prefix: ${var.lakeflow_schema_prefix}
      libraries:
        - notebook:
            path: ${workspace.root_path}/${var.lakeflow_output_path}/pipelines/notebooks/entities/010-Stage/Sales/Product/ProductDescription