resources:
  pipelines:
    lakeflow_entity_stage_sales_order_salesorderdetail:
      name: ${var.job_name_prefix}Lakeflow Entity stage Sales_Order_SalesOrderDetail
      catalog: ${var.catalog_name}
      schema: ${var.lakeflow_schema_prefix}stage
      serverless: true
      channel: PREVIEW
      configuration:
        catalog_name: ${var.catalog_name}
        schema_prefix: ${var.lakeflow_schema_prefix}
      libraries:
        - notebook:
            path: ${workspace.root_path}/${var.lakeflow_output_path}/pipelines/notebooks/entities/010-Stage/Sales/Order/SalesOrderDetail