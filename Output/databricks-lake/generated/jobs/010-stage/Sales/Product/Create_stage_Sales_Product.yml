resources:
  jobs:
    Create_stage_Sales_Product:
      name: ${var.job_name_prefix}Create Staging Data Layer Sales Product
      max_concurrent_runs: 1
      parameters:
        - name: environment
          default: ${var.environment}
        - name: catalog_name
          default: ${var.catalog_name}
        - name: schema_prefix
          default: ${var.schema_prefix}
        - name: owner
          default: ${var.owner}
        - name: workspace_host
          default: ${workspace.host}
        - name: job_name
          default: "{{job.name}}"
        - name: job_run_id
          default: "{{job.run_id}}"
      permissions:
        - group_name: ${var.permission_group_name}
          level: "CAN_MANAGE_RUN"
      job_clusters:
        - job_cluster_key: cluster_extra_small
          new_cluster: ${var.cluster_extra_small}
      tasks:

        - task_key: stage_sales_product_product
          notebook_task:
            notebook_path: ${workspace.root_path}/${var.notebook_path}/010-Stage/ddl/Sales/Product/Product
            source: WORKSPACE
          job_cluster_key: cluster_extra_small

        - task_key: stage_sales_product_productcategory
          notebook_task:
            notebook_path: ${workspace.root_path}/${var.notebook_path}/010-Stage/ddl/Sales/Product/ProductCategory
            source: WORKSPACE
          job_cluster_key: cluster_extra_small

        - task_key: stage_sales_product_productdescription
          notebook_task:
            notebook_path: ${workspace.root_path}/${var.notebook_path}/010-Stage/ddl/Sales/Product/ProductDescription
            source: WORKSPACE
          job_cluster_key: cluster_extra_small

        - task_key: stage_sales_product_productmodel
          notebook_task:
            notebook_path: ${workspace.root_path}/${var.notebook_path}/010-Stage/ddl/Sales/Product/ProductModel
            source: WORKSPACE
          job_cluster_key: cluster_extra_small

        - task_key: stage_sales_product_productmodelproductdescription
          notebook_task:
            notebook_path: ${workspace.root_path}/${var.notebook_path}/010-Stage/ddl/Sales/Product/ProductModelProductDescription
            source: WORKSPACE
          job_cluster_key: cluster_extra_small
